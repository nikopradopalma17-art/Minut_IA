# Ronda 2 Execution Plan

## Constraints

- Preserve existing uncommitted work from Claude and incorporate it only in the task it belongs to.
- Keep the visual speaker-ID prototype dormant; T5 is the local audio diarization path only.
- The application name remains `MinutIA`.
- Use Impulso brand values and Bento A only after T5 is verified.

## Task 1: T5 speaker diarization

- [ ] Treat the current camelCase and diarization-progress work as the starting patch; do not discard it.
- [ ] Add focused regression coverage for frontend Tauri argument payloads and the T5 user-feedback helper, watching it fail before any new production change.
- [ ] Keep top-level Tauri arguments camelCase; preserve `request.template_id` for the nested Serde request.
- [ ] Verify model download progress listener lifecycle, localized recoverable errors, model directory, and live `speaker` persistence through SQLite, chips, rename, reload, and minutes attribution.
- [ ] Run targeted Rust diarization tests and frontend tests; commit only T5 files.

## Task 2: T11 templates

- [ ] Keep exactly `reunion_diaria`, `minuta_corporativa`, and `reunion_cliente` built-ins; rename the client template JSON and remove the remaining eight JSON files.
- [ ] Set `minuta_corporativa` everywhere as the default and update template registry/tests/docs.
- [ ] Correct top-level Tauri `templateId` calls, preserving the nested save request's `template_id` field.
- [ ] Add regression coverage and commit only T11 files.

## Task 3: T12 i18n

- [ ] Localize the specified recovery, summary, retranscription, audio-import, model-manager, and overlay surfaces in English and Spanish.
- [ ] Add a locale-parity check and use it in verification.
- [ ] Incorporate the existing localized summary changes; commit only T12 files.

## Task 4: T13/T14 visual system and brand

- [ ] Consolidate Tailwind to one active configuration; load Jost for headings and Inter for UI/body.
- [ ] Implement semantic Impulso HSL tokens, Card and BentoGrid primitives, keyboard focus, reduced motion, and core-system token migration.
- [ ] Implement Bento A on Inicio, Reuniones, Meeting Details, recording controls, and compact sidebar navigation.
- [ ] Add transparent Impulso logo variants, the exact Impulso attribution, and a blue MinutIA SVG mark.
- [ ] Verify responsive and accessibility states; commit T13/T14 files.

## Task 5: Integration verification

- [ ] Run Rust compilation/tests, template tests, locale parity, frontend validation, and visual checks.
- [ ] Run Windows Tauri diarization E2E with microphone and system audio. Do not mark T5 complete without recorded proof.
